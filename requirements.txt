# CLI & Configuration
click>=8.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging
python-json-logger>=2.0.7

# Image Processing (TGA/BMP/JPG → PNG)
Pillow>=10.0.0

# Audio Conversion (WAV/MP3 to OGG; requires ffmpeg binary on PATH)

# XML handling (templates, actors)
lxml>=4.9.0

# Code Quality (dev)
ruff>=0.1.0
pytest>=7.0.0
pytest-cov>=4.0.0
